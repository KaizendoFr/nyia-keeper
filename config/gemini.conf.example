# Nyia Keeper Gemini Assistant Configuration
# Configuration for the Google Gemini AI assistant

# === ASSISTANT IDENTITY ===
ASSISTANT_NAME="gemini"
ASSISTANT_CLI="gemini"

# === DOCKER CONFIGURATION ===
BASE_IMAGE_NAME="nyiakeeper-gemini"  # Base name without tag
DOCKERFILE_PATH="docker/gemini"
CONFIG_FILENAME="GEMINI.md"
DEFAULT_USER="node"
BASE_DOCKER_IMAGE="node:22-alpine"

# === DEV IMAGE SETTINGS ===
ALLOW_DEV_IMAGES="true"
FORCE_PRODUCTION="false"

# === PROJECT CONTEXT ===
CONTEXT_DIR_NAME=".nyiakeeper/gemini"
CONFIG_DIR_NAME=".nyiakeeper/gemini"


# === AUTHENTICATION ===
AUTH_METHOD="auto"  # Supports multiple auth methods (OAuth, API key, Vertex AI)
API_KEY_ENV="GEMINI_API_KEY"  # Google AI Studio API key

# === OPTIONAL CONFIGURATION ===
# Required for some account types (Workspace, enterprise, outside supported regions)
# export GOOGLE_CLOUD_PROJECT="your-project-name"

# === RAG SETTINGS ===
# Embedding model for codebase search (requires Ollama on host)
# Options: nomic-embed-text (768d, fast), mxbai-embed-large (1024d, accurate)
# To use: nyia-gemini --rag
# Install model: ollama pull nomic-embed-text
NYIA_RAG_MODEL="nomic-embed-text"

# === DISPLAY METADATA (Plan 35 — shown by `nyia list`) ===
ASSISTANT_DISPLAY_NAME="Gemini"
ASSISTANT_FULL_NAME="Gemini CLI"
ASSISTANT_DESCRIPTION="Google's open-source terminal AI agent, powered by Gemini models"
ASSISTANT_PROVIDER="Google"
